(function(){const T='three-column-dark';function init(){if(document.documentElement.getAttribute('data-theme')!==T)return;initResize();initPanels()}
function initResize(){document.querySelectorAll('[data-resize]').forEach(h=>{let start=0;h.addEventListener('mousedown',e=>{start=e.clientX;document.addEventListener('mousemove',onMove);document.addEventListener('mouseup',()=>document.removeEventListener('mousemove',onMove))});function onMove(e){const d=e.clientX-start;h.parentElement.style.width=(parseInt(getComputedStyle(h.parentElement).width)+d)+'px';start=e.clientX}})}
function initPanels(){document.querySelectorAll('.glass-card').forEach(c=>{c.addEventListener('mouseenter',()=>c.style.borderColor='oklch(from var(--accent) l c h / 0.2)');c.addEventListener('mouseleave',()=>c.style.borderColor='')})}
document.readyState==='loading'?document.addEventListener('DOMContentLoaded',init):init();new MutationObserver(()=>init()).observe(document.documentElement,{attributes:true,attributeFilter:['data-theme']})})();
