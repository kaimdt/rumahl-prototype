[package]
name = "rumahl-shared-config"
version = "0.1.0"
edition = "2021"
description = "rumahl environment and runtime configuration helpers"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
uuid = { version = "1.0", features = ["v4"] }

[lib]
name = "rumahl_shared_config"
path = "src/lib.rs"
