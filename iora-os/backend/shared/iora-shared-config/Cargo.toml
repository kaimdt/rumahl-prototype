[package]
name = "iora-shared-config"
version = "0.1.0"
edition = "2021"
description = "IORA environment and runtime configuration helpers"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
uuid = { version = "1.0", features = ["v4"] }

[lib]
name = "iora_shared_config"
path = "src/lib.rs"
