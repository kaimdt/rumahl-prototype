[package]
name = "iora-shared-types"
version = "0.1.0"
edition = "2021"
description = "Stable foundational types shared by IORA services"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lib]
name = "iora_shared_types"
path = "src/lib.rs"
